---
title: Home
layout: 'base.liquid'
---
    
        {%- for article in collections.featured -%}
            {% include modules/splitHero %}
        {%- endfor -%}

<!--         <h2 class="categoryTitle">Kultur</h2>

        <section class="teaserList teaserTiles">

            {% for article in collections.culture limit: 4 %}
                {% if forloop.first %}
                    {% include modules/bigTile %}
                {% else %}
                    {% include modules/smallTile %}
                {% endif %}
                
            {% endfor %}

        </section> -->

        

   <!--      <h2 class="categoryTitle">Art & Media</h2>

        <section class="teaserList teaserTiles noMargin">

            {% for article in collections.art limit: 4 %}
                {% include modules/mediumTile %}
            {% endfor %}

            
        </section> -->
        <script>
            console.log('{{ collections.tagsList }}');
        </script>
        {%- for tag in collections.tagsList -%}

            <h2 class="categoryTitle">{{ tag }}</h2>
            <section class="teaserList teaserTiles noMargin">

                {% for article in collections[tag] limit: 4 %}
                    {% include modules/mediumTile %}
                {% endfor %}
    
                
            </section>

        {%- endfor -%}